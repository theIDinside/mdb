[workspace]
members = [
	"midas",
	"cli",
	"linuxwrapper",
	"repl",
	"server",
	"debug_info"
]

[profile.dev]
opt-level = 0               # Use slightly better optimizations.
overflow-checks = true     # Disable integer overflow checks.